apiVersion: v1
kind: Secret
metadata:
  name: pipenodes-secrets
  namespace: pipenodes
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_ME"
  OPENROUTER_API_KEY: "REPLACE_ME"
  PIPELEAP_LICENSE_KEY: "REPLACE_ME"
